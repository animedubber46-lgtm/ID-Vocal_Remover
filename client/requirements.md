## Packages
date-fns | Formatting timestamps for logs
framer-motion | Smooth animations for log entries and status cards
clsx | Utility for conditional classes (often needed with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes

## Notes
The dashboard connects to /api/logs to fetch bot activity.
The status is inferred from the latest logs or a mock status for now.
Design is strictly dark mode professional.
